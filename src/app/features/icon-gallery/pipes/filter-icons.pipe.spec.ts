import { FilterIconsPipe } from './filter-icons.pipe';

describe('FilterIconsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIconsPipe();
    expect(pipe).toBeTruthy();
  });
});
